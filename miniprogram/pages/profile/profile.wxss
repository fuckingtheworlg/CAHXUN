/* ─── Login Page ─── */
.login-page {
  min-height: 100vh;
  background: linear-gradient(160deg, #4A90D9 0%, #2c5f9e 50%, #1a3a66 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 0 60rpx;
  padding-bottom: var(--tab-bar-area);
}

.login-hero {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 64rpx;
}

.login-logo-text {
  width: 140rpx;
  height: 140rpx;
  border-radius: 50%;
  background: rgba(255,255,255,.15);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  font-weight: 700;
  color: #fff;
  letter-spacing: 2rpx;
  margin-bottom: 24rpx;
  border: 4rpx solid rgba(255,255,255,.3);
}

.login-title {
  font-size: 44rpx;
  font-weight: 700;
  color: #fff;
  margin-bottom: 16rpx;
}

.login-subtitle {
  font-size: 26rpx;
  color: rgba(255,255,255,.65);
  text-align: center;
}

.login-features {
  display: flex;
  gap: 32rpx;
  margin-bottom: 80rpx;
}

.feature-item {
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255,255,255,.12);
  border-radius: 20rpx;
  padding: 24rpx 20rpx;
  flex: 1;
}


.feature-text {
  font-size: 22rpx;
  color: rgba(255,255,255,.85);
  white-space: nowrap;
}

.wx-login-btn {
  width: 100% !important;
  height: 96rpx;
  background: #07c160 !important;
  color: #fff !important;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 48rpx !important;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none !important;
  letter-spacing: 2rpx;
  padding: 0 !important;
  margin: 0 !important;
  line-height: 96rpx !important;
}

.wx-login-btn::after {
  border: none;
}

.wx-login-btn[disabled] {
  background: rgba(7,193,96,.6) !important;
  color: rgba(255,255,255,.8) !important;
}

.login-tip {
  font-size: 22rpx;
  color: rgba(255,255,255,.4);
  margin-top: 24rpx;
}

/* ─── Profile Page ─── */
.profile-page {
  min-height: 100vh;
  background: linear-gradient(180deg, #f0f5ff 0%, #f5f5f5 360rpx);
  padding-bottom: calc(40rpx + var(--tab-bar-area));
}

/* ─── Header ─── */
.profile-header {
  position: relative;
  background: linear-gradient(135deg, #4A90D9 0%, #6FB1FC 60%, #a18cd1 100%);
  padding: 70rpx 0 60rpx;
  border-radius: 0 0 40rpx 40rpx;
  margin-bottom: 24rpx;
  overflow: hidden;
  box-shadow: 0 12rpx 32rpx rgba(74, 144, 217, 0.2);
}

.header-blob {
  position: absolute;
  border-radius: 50%;
  filter: blur(40rpx);
  opacity: 0.55;
  pointer-events: none;
}

.blob-1 {
  width: 280rpx;
  height: 280rpx;
  background: radial-gradient(circle, #fbc2eb 0%, transparent 70%);
  top: -80rpx;
  right: -60rpx;
}

.blob-2 {
  width: 220rpx;
  height: 220rpx;
  background: radial-gradient(circle, #feb47b 0%, transparent 70%);
  bottom: -40rpx;
  left: -40rpx;
  opacity: 0.4;
}

.blob-3 {
  width: 160rpx;
  height: 160rpx;
  background: radial-gradient(circle, #5cdb95 0%, transparent 70%);
  top: 50%;
  left: 30%;
  opacity: 0.35;
}

.header-content {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.avatar-wrap {
  position: relative;
  width: 160rpx;
  height: 160rpx;
  margin-bottom: 24rpx;
}

.avatar-ring {
  position: absolute;
  inset: -8rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, rgba(255,255,255,.8), rgba(255,255,255,.2));
  filter: blur(2rpx);
}

.avatar {
  position: relative;
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  border: 4rpx solid rgba(255,255,255,.6);
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,.12);
}

.avatar-edit {
  position: absolute;
  right: 0;
  bottom: 0;
  width: 48rpx;
  height: 48rpx;
  background: #fff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,.15);
  z-index: 2;
}

.edit-icon {
  font-size: 28rpx;
  color: #4A90D9;
  font-weight: 700;
  line-height: 1;
}

.nickname-wrap {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-bottom: 10rpx;
}

.nickname {
  font-size: 38rpx;
  font-weight: 800;
  color: #fff;
  text-shadow: 0 4rpx 12rpx rgba(0,0,0,.1);
}

.nickname-arrow {
  font-size: 32rpx;
  color: rgba(255,255,255,.7);
}

.uid {
  font-size: 22rpx;
  color: rgba(255,255,255,.75);
  background: rgba(255,255,255,.18);
  padding: 4rpx 16rpx;
  border-radius: 16rpx;
  backdrop-filter: blur(10rpx);
}

/* ─── Section Card ─── */
.section-card {
  background: #fff;
  margin: 0 24rpx 24rpx;
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 24rpx rgba(74, 144, 217, 0.06);
}

.section-title {
  font-size: 26rpx;
  font-weight: 600;
  color: #99a4b3;
  padding: 24rpx 28rpx 8rpx;
  display: flex;
  align-items: center;
}

.section-title::before {
  content: '';
  width: 6rpx;
  height: 22rpx;
  background: linear-gradient(180deg, #4A90D9, #a18cd1);
  border-radius: 3rpx;
  margin-right: 12rpx;
}

/* ─── Stats Card ─── */
.stats-card {
  margin: 0 24rpx 24rpx;
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 6rpx 24rpx rgba(74, 144, 217, 0.1);
}

.stats-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
  padding: 0;
}

.stats-grid .stats-item {
  position: relative;
  background: #fff;
  border-radius: 20rpx;
  padding: 36rpx 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10rpx;
  overflow: hidden;
}

.stats-bg {
  position: absolute;
  width: 140rpx;
  height: 140rpx;
  border-radius: 50%;
  filter: blur(30rpx);
  opacity: 0.4;
  top: -40rpx;
  right: -30rpx;
  pointer-events: none;
}

.stats-bg-1 {
  background: radial-gradient(circle, #4A90D9 0%, transparent 70%);
}

.stats-bg-2 {
  background: radial-gradient(circle, #ff7e5f 0%, transparent 70%);
}

.info-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx;
  position: relative;
}

.info-row-btn {
  width: 100%;
  background: transparent;
  border: none;
  border-radius: 0;
  text-align: left;
  font-size: 28rpx;
  font-weight: normal;
  line-height: normal;
  margin: 0;
  color: inherit;
}

.info-row-btn::after {
  border: none;
}

.info-row-hover {
  background: #f9f9f9;
}

.info-row::after {
  content: '';
  position: absolute;
  left: 28rpx;
  right: 28rpx;
  bottom: 0;
  height: 1rpx;
  background: #f0f0f0;
}

.info-row:last-child::after {
  display: none;
}

.info-row:active {
  background: #f9f9f9;
}

.info-label {
  font-size: 28rpx;
  color: #333;
}

.info-right {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.info-value {
  font-size: 28rpx;
  color: #666;
}

.info-arrow {
  font-size: 32rpx;
  color: #ccc;
}

.stats-num {
  position: relative;
  font-size: 52rpx;
  font-weight: 800;
  background: linear-gradient(135deg, #4A90D9, #a18cd1);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  line-height: 1.1;
}

.stats-grid .stats-item:nth-child(2) .stats-num {
  background: linear-gradient(135deg, #ff7e5f, #feb47b);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
}

.stats-label {
  position: relative;
  font-size: 24rpx;
  color: #99a4b3;
}

/* ─── Footer ─── */
.footer-text {
  text-align: center;
  font-size: 22rpx;
  color: #c0c8d4;
  padding: 40rpx 0 20rpx;
}

.info-row {
  background: transparent;
}

.info-arrow {
  color: #d0d6df;
}

/* ─── Modal ─── */
.modal-mask {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.modal-box {
  width: 560rpx;
  background: #fff;
  border-radius: 24rpx;
  padding: 48rpx 40rpx 36rpx;
}

.modal-title {
  font-size: 32rpx;
  font-weight: 700;
  text-align: center;
  margin-bottom: 32rpx;
  color: #333;
}

.modal-input {
  width: 100%;
  height: 80rpx;
  border: 2rpx solid #e5e7eb;
  border-radius: 12rpx;
  padding: 0 24rpx;
  font-size: 28rpx;
  margin-bottom: 32rpx;
}

.modal-input:focus {
  border-color: #4A90D9;
}

.modal-btns {
  display: flex;
  gap: 20rpx;
}

.modal-btn {
  flex: 1;
  height: 76rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 12rpx;
  font-size: 28rpx;
  font-weight: 600;
}

.modal-btn.cancel {
  background: #f3f4f6;
  color: #666;
}

.modal-btn.confirm {
  background: #4A90D9;
  color: #fff;
}

.modal-btn:active {
  opacity: .8;
}
